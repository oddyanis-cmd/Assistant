# Core runtime (light — no ML). Enough to run the API + tests with a fake engine.
fastapi>=0.111,<0.116
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
httpx>=0.27
numpy>=1.26
pillow>=10.0
