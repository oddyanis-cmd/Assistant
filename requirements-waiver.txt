# Minimal dependencies for the standalone Katara Club waiver system
# (use this instead of requirements.txt when deploying only the waiver app)
fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.31
pydantic-settings==2.3.4
python-multipart==0.0.9
httpx==0.27.0
reportlab==4.5.1
qrcode[pil]==8.2
arabic-reshaper==3.0.1
python-bidi==0.6.10
